name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Download Dalamud
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri https://github.com/Dalamud-DailyRoutines/Dalamud/releases/latest/download/latest.7z -OutFile latest.7z
          $sevenZip = "$env:ProgramFiles\7-Zip\7z.exe"
          if (!(Test-Path $sevenZip)) {
            $sevenZip = "7z"
          }
          $paths = @(
            "$env:AppData\XIVLauncher\addon\Hooks\dev",
            "$env:AppData\XIVLauncherCN\addon\Hooks\dev"
          )
          foreach ($path in $paths) {
            New-Item -ItemType Directory -Force -Path $path | Out-Null
            & $sevenZip x latest.7z "-o$path" -y
            Write-Host "Dalamud extracted to: $path"
          }
          Remove-Item latest.7z -Force

      - name: Build Release
        shell: pwsh
        run: dotnet build ICE/ICE.csproj -c Release -v minimal

      - name: Ensure artifact exists
        shell: pwsh
        run: |
          $source = 'ICE/bin/Release/ICE-bld/latest.zip'
          $target = 'ICE/bin/Release/ICE-bld/ICE.zip'
          if (!(Test-Path $source)) {
            throw "未找到构建产物: $source"
          }
          Copy-Item $source $target -Force
          Get-Item $target | Format-List FullName, Length, LastWriteTime

      - name: Upload ICE.zip to release
        uses: softprops/action-gh-release@v2
        with:
          files: ICE/bin/Release/ICE-bld/ICE.zip
          name: ${{ github.ref_name }}
          fail_on_unmatched_files: true
          append_body: true
          overwrite_files: true
